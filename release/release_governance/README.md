# Release Governance

Release is blocked unless governance validation, cost governance validation, extension validation, compatibility validation, package build validation, README rendering validation, and twine validation all pass.