# Retrieval-First Guide

Build a manifest, select relevant context, prune stale artifacts, and preserve active requirements, changed files, latest checkpoints, and active decisions only.

Full repository context is not a default workflow.