# Adapter Development Guide

Adapters bridge consumer runtime and AI Development OS. They may mention project names, but shared runtime must not import adapters.

Adapters should declare required capabilities, supported OS version, and extension requirements.